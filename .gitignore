# Environment files
.env

# Build artifacts
result
result-*

# Nix
.direnv/

# Workspace (user content)
workspace/

# Project mounts config
.projects

# Docker compose override (generated)
docker-compose.override.yml

# OS files
.DS_Store
*.swp
*~

# IDE
.vscode/
.idea/

# Config files copied into Docker image (not sensitive, but build artifacts)
# These are tracked in git since they define container behavior

# SSH host keys (generated at runtime, contain secrets)
ssh_host_*
